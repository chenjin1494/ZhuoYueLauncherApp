# 万能转发器 使用说明

版本：v7.20（完整操作日志与 Shizuku 输出）
包名：com.helper.urlfeeder
开发：chenjin1494
适用设备：Lenovo TB337FC，ZUI16 / Android 13

---

## 界面结构

主界面按任务拆为五个区域。平板横屏使用左侧导航，窄屏使用顶部导航；功能与设置数据完全共用。

### 总览

总览只保留高频操作和实时状态：

- 输入、粘贴并打开网址。
- 一键恢复联网。
- 检测白名单外网络。
- 清理后台进程。
- 打开 ADB / USB 调试入口。
- 查看 Shizuku、网络守护、悬浮控制和默认浏览器状态。
- 进入完整状态总览并生成隐私安全的诊断报告。
- 再次打开历史网址。
- 查看、复制或清理最近活动日志。

### 应用

- 按应用名称或包名搜索。
- 点击应用直接启动。
- 长按应用打开详情，可启动或发起卸载。
- 在列表顶部继续下拉可刷新应用清单。

### 设备

设备页负责需要系统权限或网络控制的模块：

- Shizuku 授权、启动引导、重新检测和启动命令复制。
- 无障碍截图辅助设置。
- 切换 Lawnchair、恢复卓越桌面、查询桌面状态。
- 设置默认桌面。
- 启动或停止网络守护。
- 卓越上报域名拦截。
- 生成并导入 FlClash 覆写脚本，使 FlClash 接管相同的域名拦截规则。
- 防火墙 IP 规则拦截与一键复原。
- 防火墙规则管理。
- 持续抑制卓越监控。

FlClash 导入方式：打开 `设备 → 网络与防护 → 导入 FlClash 覆写`，选择“复制并打开”，然后在 FlClash 的覆写脚本编辑器中粘贴并保存。也可选择“分享脚本文件”导出生成的 `.js` 文件。

### 控制

控制页集中管理悬浮球：

- 授权、启动或停止悬浮球。
- 调整球体大小。
- 显示或隐藏音量、亮度、静音和锁屏快捷动作。
- 添加、删除和排序自定义应用。
- 调整“前往”和“工具”菜单中的动作顺序。

悬浮球手势：

- 单击：打开或收起菜单。
- 双击：截图。
- 长按：清理后台。
- 拖动：移动位置，松手后自动贴边。

悬浮菜单采用固定的两级结构：

- **前往**：主页、万能转发器。
- **工具**：清理后台、截图、开放网络，以及可选的设备快捷动作。
- **应用**：用户添加的应用；长按应用可改名或移除。
- 一级中心按钮用于收起，二级中心按钮用于返回一级。

### 系统

- 状态总览：集中查看权限、服务、网络控制、系统关联、备份、审计、存储和电量。
- 诊断报告：支持预览、复制、导出或作为文本文件分享；Android 10+写入 `下载/万能转发器诊断`，Android 7–9 由系统文件保存器选择位置。
- 设备自检与定向修复。
- 完整操作日志：筛选、搜索、复制、导出和清理 Activity、Intent、Shizuku、服务、网络、防火墙、更新、截图及悬浮球操作。
- 查看版本与应用信息。
- 设置默认浏览器或打开系统默认应用设置。
- 切换界面背景和内容表面透明度。
- 设置桌面壁纸预设。
- 备份或恢复全部设置。
- 从 GitHub 检查并安装签名一致的新版本。

## 完整操作日志

打开 `系统 → 诊断与记录 → 完整操作日志` 查看。每条记录包含毫秒时间、事件序号、分类、动作、结果、耗时、线程和多行详情。

- Activity：创建、前后台切换、销毁、新 Intent、启动目标和返回结果。
- Intent：action、component、package、data、MIME、flags、categories 和 extras 键名；不会自动展开 extras 值。
- Shizuku：连接、掉线、授权请求与结果、完整命令、退出码、stdout、stderr 和异常堆栈。
- 服务：启动、前台启动、停止、绑定、解绑及服务生命周期。
- 设备操作：网络检测、自动开网、防火墙 Binder 调用、VPN、截图、后台清理、更新和悬浮球手势。
- 日志同时输出到 Android `logcat` 的 `WFT-Operation` 标签；应用内日志最多约 8 MB，达到上限后保留最近约 6 MB。
- 导出的日志是可直接阅读的 UTF-8 文本，Android 10+保存在 `下载/万能转发器日志`。

完整日志可能包含访问地址、URL 查询参数中的凭据或令牌、包名、命令参数、防火墙规则内容，以及命令输出中的个人、账户和系统数据。诊断报告仍只包含日志数量统计，不包含日志正文；分享完整日志前应先检查内容。

---

## 重要说明

- Shizuku 重启设备后通常需要重新启动。
- 防火墙写入会校验厂商接口返回结果，失败不会显示为成功。
- 防火墙拦截会在修改前保存规则快照，用于一键复原。
- 设置备份兼容旧版本的 `pf` 偏好键，不会因 v7.20 功能更新而失效。
- 诊断报告不包含网址历史、剪贴板、密钥、规则正文、设置值或审计详情。
- Android 同时只能运行一个 VPN；FlClash 运行时内置 VPN 会暂停，导入的覆写脚本负责执行相同的域名拦截。
- 更新包会校验包名和签名证书，避免安装不兼容 APK。
- 本工具仅用于合法的设备管理与学习研究，请遵守设备管理方规定。
