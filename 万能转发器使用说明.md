# 万能转发器 使用说明

版本：v5.95（D=5.90、F=5.91、E=5.92、规则管理=5.93、审计日志=5.94、规则写入结果校验=5.95）
包名：com.helper.urlfeeder
开发：chenjin1494 · 面向 Lenovo TB337FC（ZUI16 / Android 13）学习平板调试

---

## 主界面（🏠 首页）

1. **输入网址**（自动补 `https://`），可选：
   - 「🚀 内置白名单浏览器打开」→ 交给 com.zy.ai.browser（该 UID 白名单全通，任意网站可开）
   - 「🟢 Chrome」→ 用 Chrome 打开
   - 「📋 粘贴」→ 读剪贴板填入
2. **「🔓 一键开网」**：绑定管控防火墙导出服务（RootUTService），清空 per-UID 白名单规则，让非白名单 App 恢复联网。重启后规则重建需再点。
3. **「🌐 检测」**：实测能否连接 4 个白名单外站点：`bilibili.com / bing.com / qq.com / 163.com`（全通=开网有效；全被拦=需一键开网）。结果会记入下方运行日志。
4. **「🧹 一键清理后台」**：用系统 API 结束后台/缓存的应用进程（**不依赖 Shizuku**）；当前前台、系统应用以及防火墙/桌面/白名单浏览器不杀，处理个数记入运行日志。悬浮球菜单也有同款「🧹 清理后台」项（可排序，默认在「最近」后）。
5. **「📋 运行日志」**：记录开网/检测/守护/清理后台/打开链接/打开与卸载应用等操作（时间戳、最多 80 条、重启保留）。右上「清空 / 复制」可清空或复制到剪贴板。

## 应用页（📱 应用）

- 顶部**搜索框**：按名称或包名过滤，✕ 一键清空
- 列表在顶部**继续下拉 = 刷新**（弹簧回弹 + 旋转箭头 + 到位震动）
- **点按** = 打开应用；**长按** = 详情弹窗（版本/UID/入口），可「打开 / 🗑卸载 / 关闭」
- 卸载走系统确认框；卸载本应用自身会被拦截提示

## 设置页（⚙️ 设置）

- **📖 使用说明**：内置简版说明
- **🎨 界面背景**：渐变星空 / 薄荷 / 深蓝 / 系统壁纸毛玻璃
- **🧊 玻璃透明度**：5 档实时调整（很透明→最实），即时重建生效
- **🛡 网络守护**：开启后常驻前台服务，被拦自动清规则开网；开机自启（BootReceiver）；重启后若未自动拉起，手动点一次「启动网络守护」
- **🛠 桌面管理（Shizuku）**：授权后可在平板上直接
  - 切到 Lawnchair 桌面（停用卓越Launcher）
  - 恢复卓越Launcher 桌面
  - 查询桌面状态
  - 前提：安装 Shizuku 并以 adb 启动一次 + App 内「🔑授权」；**设备重启后 Shizuku 需重新 adb 启动**
  - **📋 复制 Shizuku 启动命令**：一键把下面这条命令复制到剪贴板（发给电脑端执行即可）
    `adb shell sh /storage/emulated/0/Android/data/moe.shizuku.privileged.api/start.sh`
  - **📶 打开 Shizuku**：直接跳到 Shizuku 应用（用「通过无线调试启动」的引导页）
  - **♻️ 检测并重连 Shizuku**：立即 ping 一次；未运行→提示启动命令；未授权→直接弹授权；正常→跑 `id` 自检
  - **Shizuku 保活提醒**（网络守护内）：守护会盯着 Shizuku 状态，**掉线**发通知「⚠ Shizuku 已掉线」、**恢复**发通知「✓ Shizuku 已恢复（已授权/未授权）」，无需手动查看

- **🪄 悬浮球导航**：渐变小圆球（可设 小36/中46/大56/特大68，点选即时生效；功能项：主页/打开主界面/清理后台/截图/开网（可排序；返回/最近已移除）；自定义应用不再占主环，统一收进二级「📱 应用」菜单：同样是居中半透明圆盘，应用真实图标绕一圈（图标+名称），盘心「‹ 返回」回主轮盘，点按直达，长按图标可改名/移除；一二级切换带缩放淡入淡出过渡动画，收起同样缩小淡出）。按住拖动、松手自动吸附屏幕左右边缘并留边距（中间松手平滑滑回最近边缘；停靠边与高度会记忆，重启后还原）；点按后在屏幕中央弹出半透明圆形轮盘（黑色 iOS 风），功能项绕一圈、中心「✕ 收起」，点圆盘以外/四角自动收起（收起同样带缩小淡出动画）；外圈整块按扇区可点、中间圆整块可点（不用点准小图标）；功能项已去掉白色圆底，图标与名称直接浮在盘面（按扇区尺寸等比放大，填得更满），仅中间圆保留白底；界面字体内置随 APK：英文/数字=Nerd Font Mono(Fira Code)，中文=思源黑体 Source Han Sans CN，emoji 由系统字体渲染；截图三级优先：①无障碍截图（Android 11+，在系统设置里一次性开启「万能转发器·截图辅助」，之后无需 Shizuku、无授权框、无系统过场动画）②Shizuku 静默 screencap ③系统录屏授权(MediaProjection，会有一次系统过场黑条)；图片统一存到相册 Pictures/Screenshots；截图时会自动临时隐藏悬浮球（球不会被拍进画面），抓完立即恢复
  - **悬浮球手势**：单击 = 开/收轮盘；双击 = 截图；长按约 0.5 秒 = 清理后台；拖动 = 调整位置并自动吸附边缘
  - **🎛 快捷动作盘**（主轮盘上的「🎛 快捷」项，可在设置里隐藏）：二级圆盘，6 个动作——🔊 音量+ / 🔉 音量- / 🔇 静音 / ☀ 亮度+ / 🌥 亮度- / 🔒 锁屏。走 Shizuku shell（`input keyevent 24/25/164/26`、`settings put system screen_brightness`，亮度按 ±28 步进并限制 5–255）；Shizuku 未运行时会提示先启动
- **🛡 卓越监控治理（设置 → 桌面管理 下方）**：
  - 「拦截卓越上报域名（VPN·只拦上报/追踪）」：VPN 仅接管 `com.zy.ai.launcher` 的流量，且**只对上报/追踪主机的 IP 加路由**（`dev01.supagent.cn`、`*supagent.cn`、OAID 标识服务等），这些包进隧道后丢弃；**其它云端流量（AI/内容/更新/支付）不经过 VPN，功能不受影响**。每 5 分钟重新解析域名，IP 变化自动重建隧道
  - 「🔥 防火墙IP规则拦截（不占 VPN·可一键复原）」：调用厂商管控防火墙(`cn.com.microtrust.firewall`/`IAFWService`)的 `addBlackRule`，把上报/追踪 IP 写进**设备级**黑名单（对所有应用生效），因此**不占用 VPN 槽位**，可与其它 VPN 共存；开启前会**快照**原有黑/白名单规则，点同一开关即`clearIpHostRules`+按快照回写=**一键复原**（本机原 IP/主机规则为空，复原即清空我们加的规则）。注意：Adb「一键开网」会清规则，可能顺带清掉这些 IP 规则，需要时重新开启即可
  - 「🧱 管控防火墙规则管理」：独立页面读取当前黑/白名单，可添加、删除、清空与套用上报 IP 预设。**每次写操作前自动快照黑/白规则**，可一键恢复最近快照；操作在后台执行并写入审计日志
  - 「🧾 本地审计日志」：与首页运行日志分离，按时间/分类/结果记录关键操作，内部文件限制约 512 KiB；支持分类筛选、搜索、复制、导出到 `Download/万能转发器备份` 与确认清空
  - VPN 自动让位：当检测到**其它 VPN 正在运行**时，本拦截会挂起（设置里显示"已暂停"），对方关闭后 15–30 秒内**自动恢复**；appops 降权层不受 VPN 影响，始终有效
  - 「持续抑制卓越监控（可选·彻底模式）」：配合 Shizuku 定期 `am force-stop com.zy.ai.launcher`，**会让卓越完全不可用**（建议同时把桌面切到 Lawnchair）；只在你想彻底停掉它时开启
  - 「应用使用时长 / 屏幕画面」为**源头阻断**：系统 appops 拒绝卓越的 `GET_USAGE_STATS`（读不到使用时长）与 `PROJECT_MEDIA`（无法建立屏幕采集投影）。可用 adb 施加：
    `adb shell appops set com.zy.ai.launcher GET_USAGE_STATS deny`
    `adb shell appops set com.zy.ai.launcher PROJECT_MEDIA deny`
  - 背景：卓越Launcher 具备 `UsageTrackingService`+`AppUsageReportManager`（应用使用时长）、`ScreenCaptureService`+MediaProjection（屏幕画面）、LauncherAtom 行为日志、极光推送上报等能力，上报相关域名含 `dev01.supagent.cn`、腾讯COS测试桶、OAID 服务等

- **💾 备份 / 恢复 / 更新**：
  - **💾 备份设置**：把全部设置（开关、排序、悬浮球位置/大小、运行日志等）导出为文本，**双写**两处：`下载/万能转发器备份/万能转发器设置备份_时间.txt`（用户可见，可拷到电脑）与 App 专属目录 `Android/data/com.helper.urlfeeder/files/backup/`
  - **📥 从备份文件恢复设置**：App 内直接列出所有备份（按时间倒序）→ 点一条即恢复。**不依赖系统文件选择器**（本机管控已把 `com.android.documentsui` 停用，系统选择器打不开）
  - **⬆️ 检查更新**：优先从仓库 `dist/` 的公开 raw 直链下载，失败时尝试 GitHub Release：
    `https://raw.githubusercontent.com/chenjin1494/ZhuoYueLauncherApp/main/dist/urlfeeder-latest.apk`
    `https://github.com/chenjin1494/ZhuoYueLauncherApp/releases/latest/download/urlfeeder-latest.apk`
    → 校验 APK 包名、签名与版本号 → 有新版则弹窗「立即安装」（走系统安装器，首次需允许「安装未知应用」）。固定签名保证可覆盖安装并保留设置；检查来源、版本与失败原因会写入本地日志
- **⚡ 默认应用/工具**：设默认浏览器（需授权一次，守护保持）、设为桌面（角色授权）、打开默认应用设置、手动一键开网、一键打开 ADB

---

## 一键开网原理（简要）

管控 = 内核按 UID 白名单的 netfilter 规则，由
`cn.com.microtrust.firewall`（RootUTService，导出且无鉴权）→ root HAL
`vendor.microtrust.hardware.afw@2.0-service` 执行。
转发器借用该无鉴权服务调用：clearRules(9) / clearAppRules(10) / setEnable(1) / writeToFile(11)，把规则清空 → 全 App 恢复联网，直到重启后规则重建。

> 注意：若 `cn.com.microtrust.firewall` 被停用（pm disable-user），一键开网会静默失效（bind not found）。遇到开网无反应，先 `adb shell pm enable cn.com.microtrust.firewall`。

---

## 设备相关已知现象（实测）

- 卓越Launcher：system app（uid1000），会钉 HOME 解析层、自动关 ADB、开机重置若干设置；其导出组件为 0（无 firewall 那种可借道接口）。
- 停用卓越（disable-user）后，防火墙规则高频重推会停止；重启后若它被重新启用，重推与桌面抢占可能恢复。
- 全面屏手势：navigation_mode=2 + gestural overlay 正常时侧滑返回可用；底部手势条由 SystemUI 渲染，若异常不可用可考虑无障碍手势 App（如 EdgeGesture）替代。
- ADB 易在切桌面/锁屏时掉线：`adb kill-server && adb start-server` 后重试；必要时插拔 USB 重新授权。
- 通知栏若被系统禁用（dumpsys statusbar 见 mDisabled1=0x9f0000 类），属管控策略下发，普通 App 无法解除，需组织侧开关。
- 文件选择器（`com.android.documentsui`）被管控停用 → 任何 App 的「打开文件/另存为」都会失败；本应用的备份/恢复因此改为内置列表，不调用系统选择器。
- 本地构建用 `./build-local.sh 万能转发器_vX.Y.apk`（固定 keystore `build/k.ks`），CI 用仓库内 `keystore/urlfeeder.jks`——两者是同一把密钥，因此 `adb install -r` 与 App 内自更新都能覆盖安装并保留设置。

## 合规提醒

以上能力多用于**规避 org 白名单网络/桌面管控**，属对抗性使用，随时可能被组织端策略更新打断或被发现。长期使用前请确认自身权限；正式场景建议走组织解绑或合规白名单。
